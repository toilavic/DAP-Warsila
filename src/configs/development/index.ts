const development = {
    influxURL: process.env.INFLUX_URL_DEFAULT,
    influxToken: process.env.INFLUX_TOKEN_DEFAULT,
    influxOrg: process.env.INFLUX_ORG_DEFAULT,
    influxBucket: process.env.INFLUX_BUCKET_DEFAULT,
}

export default development